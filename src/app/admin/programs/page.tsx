'use client';

import { Typography, Box } from '@mui/material';

export default function ProgramsAdminPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Programs
      </Typography>
      <Typography color="text.secondary">Lista de programas — próximamente</Typography>
    </Box>
  );
}
