'use client';

import { Typography, Box } from '@mui/material';

export default function SiteConfigAdminPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Site Config
      </Typography>
      <Typography color="text.secondary">Configuración del sitio — próximamente</Typography>
    </Box>
  );
}
